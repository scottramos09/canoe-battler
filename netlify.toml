# CANOE ARENA — Netlify deployment config
# The game client is fully static (public/). The multiplayer server is a
# Node+WebSocket process that MUST run elsewhere (see render.yaml for a
# free Render.com setup). Point the client at it via the CANOE_SERVER env var.
[build]
  command = "node scripts/build.js"
  publish = "public"

# The game-server host injected into the client at BUILD time by
# scripts/build.js. Point at your Render service (host only — no scheme/port).
[build.environment]
  CANOE_SERVER = "canoe-arena-server.onrender.com"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
